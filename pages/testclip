<ul class="image-list-clip">
  {% assign clip_pages = site.pages
    | where_exp: "p", "p.clip == true"
    | sort: "clip_id"
    | reverse
  %}

  {% for p in clip_pages %}
    <li>
      <a href="{{ p.url | relative_url }}">
        <img src="{{ p.clip_image | relative_url }}">
      </a>
      <div class="details">
        <a href="{{ p.url | relative_url }}">
          <span class="clip-title">{{ p.title }}</span>
        </a>
        <br><span class="publisher">{{ p.clip_publisher }}</span>
        <br><span class="pub-date">{{ p.clip_date_display }}</span>
      </div>
    </li>
  {% endfor %}
</ul>
